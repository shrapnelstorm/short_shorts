import time

class client:
    def __init__()
        
    def read_inst(filename):
        f = open(filename)
        lines = [line.strip() for line in f]
        f.close()
        return lines
    def sleep(delay):
        time.sleep(time) # it sleeps for (time) seconds
        
